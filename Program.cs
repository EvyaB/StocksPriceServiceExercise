
class Program
{
    static async Task Main()
    {
        // Create input reader, then configure with inputs
        // Create public interface class for getting data from user
        // await read inputs (forever until service closes)
    }
}
